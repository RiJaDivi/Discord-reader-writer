# Discord Crypt Wrapper (TEMP NAME)

## Installation
- install jq from https://jqlang.org/download/ if you don't have it
- git clone this repo

## Usage

### Read messages
./gitsketa.sh -r [num of messages]

### Post message
./gitsketa.sh -p [message]
